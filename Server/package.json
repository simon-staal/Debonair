{
  "name": "rest",
  "version": "0.1.0",
  "description": "REST API for handling requests from frontend REACT app and communicating with ESP32 via MQTT",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Simon Staal",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mqtt": "^4.2.6"
  }
}
